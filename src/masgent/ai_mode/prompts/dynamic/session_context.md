## Current Session Context

Session runs directory: {{runs_dir}}
Current provider model: {{provider_name}}
Recent tool executions: {{recent_tools_summary}}

### Existing Files in Session
{{file_list}}

### Generated Output Directories
{{output_dirs}}

Reference files by their relative paths from the runs directory. The `POSCAR` file at the root is the most recent primary structure.
