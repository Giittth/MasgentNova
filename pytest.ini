[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    asyncio: 异步测试
    integration: 集成测试（需要外部环境）
    slurm: 需要 Slurm 环境的集成测试